package com.example.quoteapp.screens.utils

import androidx.cardview.widget.CardView
import androidx.recyclerview.widget.RecyclerView

class QuoteViewHolder(cardView: CardView) : RecyclerView.ViewHolder(cardView)

class FavouriteViewHolder(cardView: CardView) : RecyclerView.ViewHolder(cardView)
